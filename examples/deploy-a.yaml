apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-a
  namespace: vcd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-a
  template:
    metadata:
      labels:
        app: app-a
    spec:
      containers:
      - name: app-a
        image: nginx:alpine
        ports:
        - containerPort: 80
